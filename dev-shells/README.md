### NIX SHELLS
These are needed since you can't just use normal dev tools globally like pkg-config and what not. 
These are put here so I don't bloat projects with random nix files and flakes, you can just alias into the right environment to get the extra pkgs you need. Rust should have everything any random rust project I have should need.